// examples/demo.ltl — runnable demo for ltl_ntp_client

import std::io
use ltl_ntp_client as lib

fn main() {
    io::println("ltl_ntp_client demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}