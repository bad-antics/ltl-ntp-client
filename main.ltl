// ltl-ntp-client/main.ltl
//
// SNTP (RFC 4330) time query with stratum reporting.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-ntp-client v0.1.0")
    io::println("SNTP (RFC 4330) time query with stratum reporting.")
    0
}
